import { Routes } from '@angular/router';
import { RoutePlannerComponent } from './pages/route-planner/route-planner.component';

export const routes: Routes = [
  { path: '', component: RoutePlannerComponent }
];
